const arrays = require('./arrays/arrays.js'); 

const miArreglo = [30,45,34,23];

let resultado = arrays.calcularSuma(miArreglo); 

console.log(resultado);

console.log(arrays.sumaPares(miArreglo));

// objetos

const persona = {
    "nombre" : "Juan",
    "apellido" : "Gomez"
}

console.log(persona);

